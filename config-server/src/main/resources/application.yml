server:
  port: 8097

spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/yezhimincxvxb/cloud/
          searchPaths: config
          username:
          password:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/

